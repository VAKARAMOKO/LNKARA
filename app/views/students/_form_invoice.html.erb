            <%= form_with(model:[@promo, @classroom, @student, @student.new_invoice]) do |form| %>
            <div class="form-group">
              <%= form.text_field :operation  %>
              <%= form.text_field :solde  %>
              <%= form.text_field :status  %>
            </div>

              <%= form.submit "Enregistré", class:"btn btn-outline-primary"  %>
            <% end %>
